# Figma API Configuration
# Copy this file to .env.local and fill in your values

# This is a React app, so variables must start with REACT_APP_
# Note: These variables will be exposed in the client-side code
# DO NOT put sensitive tokens here - users should input them via the form

# Optional: Default Figma API base URL (already set in the code)
# REACT_APP_FIGMA_API_BASE=https://api.figma.com/v1

# Example file ID for testing (optional)
# REACT_APP_EXAMPLE_FILE_ID=your_figma_file_id_here